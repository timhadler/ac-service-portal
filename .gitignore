# OS files
.DS_Store
Thumbs.db

# Editor folders
.vscode/
.idea/

# Logs
*.log

# Environment variables (if added later)
.env

# Node (if added later)
node_modules/

# Local agent guidance — kept out of the repo deliberately.
# Tracked until now, so it is removed with `git rm --cached CLAUDE.md`;
# the file itself stays in the working tree.
CLAUDE.md
